import express, { Request, Response, NextFunction } from 'express';

export const PhotoDelete = (request: Request, response: Response, next: NextFunction) => {
  
}

export const PhotoGet = (request: Request, response: Response, next: NextFunction) => {

}

export const PhotoPost = (request: Request, response: Response, next: NextFunction) => {
  
}

export const PhotoPut = (request: Request, response: Response, next: NextFunction) => {
  
}