import express, { Request, Response, NextFunction } from 'express';

export const AlbumDelete = (request: Request, response: Response, next: NextFunction) => {
  
}

export const AlbumGet = (request: Request, response: Response, next: NextFunction) => {

}

export const AlbumPost = (request: Request, response: Response, next: NextFunction) => {
  
}

export const AlbumPut = (request: Request, response: Response, next: NextFunction) => {
  
}